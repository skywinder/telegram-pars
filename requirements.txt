# Зависимости для парсера Telegram (обновлено для Python 3.12)
telethon==1.36.0  # Библиотека для работы с Telegram API (последняя версия)
pandas==2.2.3     # Для работы с данными (последняя стабильная версия)
python-dotenv==1.0.1  # Для загрузки переменных окружения

# Дополнительные библиотеки для новых функций
matplotlib==3.9.4 # Для создания графиков (последняя версия)
wordcloud==1.9.4  # Для создания облака слов (обновлено)
plotly==6.2.0     # Интерактивные графики (последняя версия)
emoji==2.14.1     # Для анализа эмодзи (последняя версия)
aiofiles==24.1.0  # Асинхронная работа с файлами

# Для работы с голосовыми сообщениями (опционально)
# SpeechRecognition==3.10.0  # Распознавание речи (раскомментируйте при необходимости)
# pydub==0.25.1     # Обработка аудио (раскомментируйте при необходимости)

# Веб-интерфейс (последние версии для Python 3.12)
Flask==3.1.0      # Веб-фреймворк (последняя версия)
Jinja2==3.1.5     # Шаблонизатор (последняя версия)
Werkzeug==3.1.3   # WSGI утилиты (последняя версия)
itsdangerous==2.2.0  # Безопасное подписывание данных
click==8.1.8      # Интерфейс командной строки
requests==2.31.0  # HTTP библиотека для status_monitor.py