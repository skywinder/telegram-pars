# Зависимости для парсера Telegram (совместимые с Python 3.7)
telethon==1.32.1  # Библиотека для работы с Telegram API
pandas==1.3.5     # Для работы с данными (совместимая версия)
python-dotenv==0.21.1  # Для загрузки переменных окружения

# Дополнительные библиотеки для новых функций
matplotlib==3.5.3 # Для создания графиков (совместимая версия)
wordcloud==1.9.2  # Для создания облака слов
plotly==5.10.0    # Интерактивные графики (совместимая версия)
emoji==2.8.0      # Для анализа эмодзи

# Для работы с голосовыми сообщениями (опционально)
# SpeechRecognition==3.9.0  # Распознавание речи (раскомментируйте при необходимости)
# pydub==0.25.1     # Обработка аудио (раскомментируйте при необходимости)

# Веб-интерфейс
Flask==2.3.3      # Веб-фреймворк
Jinja2==3.1.2     # Шаблонизатор
Werkzeug==2.3.7   # WSGI утилиты